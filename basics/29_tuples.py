# Tuples are immutable structures - cannot assign, change once created

numbers = (1,2,3)       # lists uses [], Tuples uses ()
print(numbers)
print(numbers[0])
# numbers[0] = 0        # this will not work, as Tuples are immutable, cannot change them            